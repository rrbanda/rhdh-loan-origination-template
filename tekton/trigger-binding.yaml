apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: loan-origination-push-binding
  namespace: loan-origination
spec:
  params:
    - name: git-url
      value: $(body.repository.clone_url)
    - name: git-revision
      value: $(body.after)
    - name: repo-name
      value: $(body.repository.name)
